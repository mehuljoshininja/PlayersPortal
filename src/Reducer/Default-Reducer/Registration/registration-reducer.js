// import {REGISTER_DATA} from '../../../Action/action-type';

export default (state ='', action) => {
    switch(action.type) {
        // case REGISTER_DATA :
        //     return Object.assign({}, state, action.data);
        default :
            return state;
    }
}